{
  "$schema": "schemas/config.schema.json",
  "build": "",
  "default_grammar": "1",
  "versions": [1, 2, 3, 4, 5],
  "out_dir": "{root}/test/build",
  "stages": {
    "lexer": {
      "cmd": ["sbt", "--batch", "--error", "run --lex {input} -o {tokens_out}"],
      "out": "{tokens_out}",
      "preprocess": [
        {"type": "json", "keep": ["kind", "line", "column"]}
      ]
    },
    "parser": {
      "cmd": ["sbt", "--batch", "--error", "run --parse {input} -o {ast_out}"],
      "out": "{ast_out}",
      "preprocess": [
        {"type": "json", "keep": ["kind", "line", "column"]}
      ]
    },
    "llvm": {
      "cmd": ["sbt", "--batch", "--error", "run --llvm {input} -o {llvm_out}"],
      "out": "{llvm_out}",
      "preprocess": [
        {"type": "llvm"}
      ]
    },
    "compiler": {
      "cmd": ["sbt", "--batch", "--error", "run --compile {exe} {input}"]
    },
    "run": {
      "cmd": ["{exe}"],
      "out": "stdout"
    }
  },
  "llvm_dis": "llvm-dis",
  "fuzz": {
    "grammar": {
      "1": "doc/grammar1.g4",
      "2": "doc/grammar2.g4",
      "3": "doc/grammar3.g4",
      "4": "doc/grammar4.g4",
      "5": "doc/grammar5.g4"
    },
    "count": 40,
    "max_tokens": 30,
    "exit": { "lexer": 0 },
    "stages": ["lexer"]
  }
}